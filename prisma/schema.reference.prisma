// ──────────────────────────────────────────────────────────────────────────────
// SCHEMA REFERENCE — For documentation only. This project uses Knex (not Prisma).
// This file documents the active MySQL tables used by the Express backend.
// Legacy Laravel tables (cache, failed_jobs, jobs, sessions, etc.) have been removed.
// ──────────────────────────────────────────────────────────────────────────────

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model anggotas {
  id           String    @id @db.Char(36)
  nama         String    @db.VarChar(255)
  nim          String    @unique(map: "anggotas_nim_unique") @db.VarChar(255)

  angkatan     String    @db.VarChar(255)
  jabatan      String?   @db.VarChar(255)
  email        String?   @db.VarChar(255)
  instagram    String?   @db.VarChar(255)
  linkedin     String?   @db.VarChar(255)
  motto        String?   @db.VarChar(500)
  foto         String?   @db.VarChar(255)
  status_aktif Boolean   @default(true)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)

  @@index([angkatan], map: "anggotas_angkatan_index")
  @@index([status_aktif], map: "anggotas_status_aktif_index")
}

model beritas {
  id           String         @id @db.Char(36)
  judul        String         @db.VarChar(255)
  slug         String         @unique(map: "beritas_slug_unique") @db.VarChar(255)
  isi          String         @db.LongText
  thumbnail    String?        @db.VarChar(255)
  published_at DateTime?      @db.Timestamp(0)
  status       beritas_status @default(draft)
  created_at   DateTime?      @db.Timestamp(0)
  updated_at   DateTime?      @db.Timestamp(0)
  deleted_at   DateTime?      @db.Timestamp(0)

  @@index([published_at], map: "beritas_published_at_index")
  @@index([slug], map: "beritas_slug_index")
  @@index([status], map: "beritas_status_index")
}

model galeris {
  id         String    @id @db.Char(36)
  judul      String    @db.VarChar(255)
  foto       String    @db.VarChar(255)
  kategori   String    @db.VarChar(255)
  tanggal    DateTime  @db.Date
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([kategori], map: "galeris_kategori_index")
  @@index([tanggal], map: "galeris_tanggal_index")
}

model pesans {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  nama       String    @db.VarChar(100)
  email      String?   @db.VarChar(100)
  isi_pesan  String    @db.Text
  is_read    Boolean   @default(false)
  read_at    DateTime? @db.Timestamp(0)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

model program_kerjas {
  id              String                @id @db.Char(36)
  nama_program    String                @db.VarChar(255)
  deskripsi       String                @db.Text
  foto            String?               @db.VarChar(255)
  tanggal_mulai   DateTime              @db.Date
  tanggal_selesai DateTime              @db.Date
  status          program_kerjas_status @default(perencanaan)
  created_at      DateTime?             @db.Timestamp(0)
  updated_at      DateTime?             @db.Timestamp(0)
  deleted_at      DateTime?             @db.Timestamp(0)

  @@index([status], map: "program_kerjas_status_index")
}

model users {
  id                String    @id @db.Char(36)
  name              String    @db.VarChar(255)
  email             String    @unique(map: "users_email_unique") @db.VarChar(255)
  email_verified_at DateTime? @db.Timestamp(0)
  password          String    @db.VarChar(255)
  remember_token    String?   @db.VarChar(100)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
}

model kegiatan {
  id              String            @id @db.Char(36)
  judul           String            @db.VarChar(255)
  deskripsi       String?           @db.Text
  tanggal_mulai   DateTime          @db.DateTime(0)
  tanggal_selesai DateTime?         @db.DateTime(0)
  lokasi          String?           @db.VarChar(255)
  kategori        kegiatan_kategori @default(lainnya)
  created_at      DateTime?         @db.Timestamp(0)
  updated_at      DateTime?         @db.Timestamp(0)
  deleted_at      DateTime?         @db.Timestamp(0)

  @@index([tanggal_mulai], map: "kegiatan_tanggal_mulai_index")
  @@index([kategori], map: "kegiatan_kategori_index")
}

model merchandise {
  id           String                @id @db.Char(36)
  nama         String                @db.VarChar(255)
  deskripsi    String?               @db.Text
  harga        Int                   @default(0)
  foto         String?               @db.LongText
  kategori     merchandise_kategori  @default(lainnya)
  is_available Boolean               @default(true)
  created_at   DateTime?             @db.Timestamp(0)
  updated_at   DateTime?             @db.Timestamp(0)
  deleted_at   DateTime?             @db.Timestamp(0)

  @@index([kategori], map: "merchandise_kategori_index")
  @@index([is_available], map: "merchandise_is_available_index")
}

// ─── Enums ──────────────────────────────────────────────────────────────────

enum beritas_status {
  draft
  published
}

enum program_kerjas_status {
  perencanaan
  berjalan
  selesai
  dibatalkan
}

enum kegiatan_kategori {
  rapat
  seminar
  sosial
  lainnya
}

enum merchandise_kategori {
  kaos
  jaket
  topi
  aksesori
  lainnya
}
